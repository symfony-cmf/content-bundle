cmf_core:
    multilang:
        locales: ['en', 'de', 'fr']

cmf_routing:
    dynamic:
        enabled: true
        persistence:
            phpcr:
                enabled: true
                route_basepaths: /test/routes

cmf_content:
    persistence:
        phpcr:
            enabled: true
            content_basepath: /test/contents
